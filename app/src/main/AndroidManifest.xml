<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--移除Glance小组件多余的权限-->
    <uses-permission
        android:name="android.permission.WAKE_LOCK"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.RECEIVE_BOOT_COMPLETED"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:node="remove" />

    <application
        android:name=".VerveDoApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VerveDo.Starting"
        tools:targetApi="37">
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.CrashActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.VerveDo" />
        <activity
            android:name=".ui.widget.upcoming.WidgetTaskCreationActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:label="@string/action_add_task"
            android:theme="@style/Theme.VerveDo" />
        <activity
            android:name=".ui.widget.upcoming.UpcomingWidgetConfigurationActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:label="@string/widget_settings"
            android:theme="@style/Theme.VerveDo">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ui.widget.all.AllIncompleteWidgetReceiver"
            android:exported="true"
            android:label="@string/label_widget_all_incomplete">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/all_incomplete_widget_info" />
        </receiver>
        <receiver
            android:name=".ui.widget.today.TodayTaskWidgetReceiver"
            android:exported="true"
            android:label="@string/label_widget_today_task">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_task_widget_info" />
        </receiver>
        <receiver
            android:name=".ui.widget.upcoming.UpcomingTaskWidgetReceiver"
            android:exported="true"
            android:label="@string/label_widget_upcoming">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/upcoming_task_widget_info" />
        </receiver>
    </application>
</manifest>
